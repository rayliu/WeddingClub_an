<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item><!-- 最顶电量状态栏. -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item -->
        <item name="android:listDivider">@color/colorPrimaryDark</item>
        <item name="android:textSize">14sp</item>

        <!--ActionBar继承的样式中，有一个属性，contentInsetStart（默认值是16dp）-->
        <item name="android:actionBarStyle">@style/CustomActionBarStyle</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle"> @style/CustomActionBarStyle</item>
    </style>

    <style name="BaseAppTheme" parent="AppTheme">

    </style>

    <style name="CustomActionBarStyle" parent="Widget.AppCompat.Light.ActionBar">
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
        <item name="contentInsetLeft">0dp</item>
        <item name="contentInsetRight">0dp</item>
    </style>

    <style name = "TabLayoutTextAppearance"  parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
